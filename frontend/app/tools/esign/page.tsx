import { EsignLanding } from "@/components/esign/EsignLanding";

export const metadata = {
  title: "Free eSignature Tool — RealProfits eSign",
  description: "Sign PDFs online for free. Upload, add signers, send. ESIGN Act and eIDAS compliant. No credit card required.",
};

export default function EsignLandingPage() {
  return <EsignLanding />;
}
